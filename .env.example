# LLM Configuration
# Primary LLM provider (default: TOGETHER)
LLM_PROVIDER=TOGETHER

# Fallback LLM provider when primary fails (default: GROQ)
LLM_FALLBACK_PROVIDER=GROQ

# API Keys
TOGETHER_API_KEY=your_together_api_key
GROQ_API_KEY=your_groq_api_key

# Examples of different configurations:
# 1. Default (Together -> Groq):
#    LLM_PROVIDER=TOGETHER
#    LLM_FALLBACK_PROVIDER=GROQ
#
# 2. Groq primary with Together fallback:
#    LLM_PROVIDER=GROQ
#    LLM_FALLBACK_PROVIDER=TOGETHER
#
# 3. Add new LLM in the future:
#    - Implement new LLM class (e.g., OpenAILLM)
#    - Add to llmFactory.ts switch case
#    - Set: LLM_PROVIDER=OPENAI
#          LLM_FALLBACK_PROVIDER=GROQ
